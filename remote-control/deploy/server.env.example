# 服务器环境变量 — 复制到 /etc/remote-control/server.env
# 权限: chmod 600 /etc/remote-control/server.env

# 强随机 Token（三端必须一致）
# 生成: openssl rand -base64 48
TOKEN=CHANGE_ME_USE_openssl_rand

# Node 只监听本地，由 nginx 对外 443
HOST=127.0.0.1
PORT=8443
NODE_ENV=production
